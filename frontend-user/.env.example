NEXT_PUBLIC_API_BASE_URL=http://localhost:5000/api

# Leaflet tiles + map search geocoding (MapTiler free tier — one key for both)
NEXT_PUBLIC_MAPTILER_API_KEY=your_maptiler_api_key

# Optional dedicated geocoders (not required if MapTiler is set)
NEXT_PUBLIC_LOCATIONIQ_API_KEY=your_locationiq_key
# NEXT_PUBLIC_GEOAPIFY_API_KEY=your_geoapify_key
